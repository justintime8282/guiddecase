# Local tooling config
.claude/

# OS / OneDrive junk
desktop.ini
Thumbs.db
.DS_Store

# Company-provided assignment (do not redistribute)
Sales Operations Manager - Home assignment.pdf

# Working/prep notes — keep local, never publish (public repo = HTML deliverables only)
*.md
